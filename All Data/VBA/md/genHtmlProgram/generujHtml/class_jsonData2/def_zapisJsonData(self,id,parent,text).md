# def zapisJsonData(self, id, parent, text):

### list of variables
radek   

### code of method
```
    def zapisJsonData(self, id, parent, text):

        radek = '                { "id": "' + id + '", "parent": "' + parent + '", "text": "' + text + '" },'

        return (radek)
```
### links

