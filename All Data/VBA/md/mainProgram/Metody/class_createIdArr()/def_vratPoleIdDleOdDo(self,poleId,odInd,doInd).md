# def vratPoleIdDleOdDo(self, poleId, odInd, doInd):

### list of variables
poleId[i]   

### code of method
```
    def vratPoleIdDleOdDo(self, poleId, odInd, doInd):

        for i in range(odInd, doInd):
            poleId[i] = odInd - 1

        return(poleId)
```
