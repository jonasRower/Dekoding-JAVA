# def vratPoleIndexu(self, poleRadku, strExp):

### list of variables
poleIndexuRadku   
radek   
if(radek   

### code of method
<pre>
 <code>
    def vratPoleIndexu(self, poleRadku, strExp):

        poleIndexuRadku = []

        for i in range(0, len(poleRadku)):
            radek = poleRadku[i]
            if(radek == strExp):
                poleIndexuRadku.append(i)
 <code>
<pre>
