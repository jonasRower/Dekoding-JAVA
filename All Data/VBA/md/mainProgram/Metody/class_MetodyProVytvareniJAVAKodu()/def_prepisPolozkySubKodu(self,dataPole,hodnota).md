# def prepisPolozkySubKodu(self, dataPole, hodnota):

### list of variables
if(hodnota !  
i   
dataPole[i]   

### code of method
<pre>
 <code>
    def prepisPolozkySubKodu(self, dataPole, hodnota):
        # prepisuje hodnotou, pouze pokud hodnota != ""
        if(hodnota != ""):
            i = -1
            for x in dataPole:
                i = i + 1
                dataPole[i] = hodnota

        return(dataPole)
 <code>
<pre>
