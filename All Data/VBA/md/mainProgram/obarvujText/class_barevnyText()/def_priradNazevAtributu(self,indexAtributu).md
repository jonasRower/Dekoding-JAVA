# def priradNazevAtributu(self, indexAtributu):

### list of variables
if(indexAtributu   
if (indexAtributu   

### code of method
```
    def priradNazevAtributu(self, indexAtributu):

        if(indexAtributu == 1):
            return ("nazevInstance")

        if (indexAtributu == 2):
            return ("nazevMetody")

        if (indexAtributu == 3):
            return ("nazevTridy")

        if (indexAtributu == 4):
            return ("volanaInstance")

        if (indexAtributu == 5):
            return ("volanaMetoda")

        if (indexAtributu == 6):
            return ("volanaTrida")
```
