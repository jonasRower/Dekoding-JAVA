# def zapisVsechnaSlovaAId(self, slovo, id):

### list of variables
if(slovo !  
slovoAId   

### code of method
<pre>
 <code>
    def zapisVsechnaSlovaAId(self, slovo, id):

        if(slovo != ""):

            slovoAId = []

            slovoAId.append(slovo)
            slovoAId.append(id)

            self.vsechnaSlovaAId.append(slovoAId)
 <code>
<pre>
