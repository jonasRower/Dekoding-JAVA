# def unique(self, list1):

### list of variables
unique_list   
if op.countOf(unique_list, x)   

### code of method
```
    def unique(self, list1):

        # initialize a null list
        unique_list = []

        # traverse for all elements
        for x in list1:
            # check if exists in unique_list or not
            if op.countOf(unique_list, x) == 0:
                unique_list.append(x)

        return(unique_list)
```
### links

