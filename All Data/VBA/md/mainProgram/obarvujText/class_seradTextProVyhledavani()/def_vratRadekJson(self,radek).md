# def vratRadekJson(self, radek):

### list of variables
jsonSlovoStr   
<a href  
jsonRadek   

### code of method
<pre>
 <code>
    def vratRadekJson(self, radek):

        jsonSlovoStr = self.vratJsonSlovoStr(radek)
          <a href="../../../../../../All%20Data/VBA/md/mainProgram/obarvujText/def___init__(self,_vsechnaSlovaAId)/jsonIdStr = '"id" ["'.md">jsonIdStr=self.vratJsonIdStr(radek)</a>

        jsonRadek = jsonSlovoStr + jsonIdStr

        return(jsonRadek)
 <code>
<pre>
